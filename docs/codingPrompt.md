# 任务1
完成后台侧-管理系统的功能开发：
1、登录、注册、用户管理功能
- 登录：使用较为简洁的风格，完成开发（用户名+密码鉴权）
- 注册：接收用户名（唯一）+密码+昵称+手机号（唯一）
- 用户管理：采用列表呈现，20/页，分页功能，支持新增/删除/修改/禁用/搜素等功能

# 任务1.1
默认后台登录用户名和密码（admin/admin888）

# 任务1.2
发现问题检测代码，修复问题。
- layui在dist/layui/目录下，需要拷贝到static/layui/目录下

# 任务1.3
后台管理系统布局优化：
- 采用Layui框架重新布局管理界面
-- 上为LOGO+系统名称
-- 下左侧为菜单区
-- 所有界面组件均采用 layui 统一组件风格：弹窗/toast/alert/表单/表格/弹层/导航等
-- 优化首页


# 任务2
完成后台侧-管理系统的功能开发：
1、AIOT设备管理
- 可以支持新增/删除/修改/查询/分页 （20/页）功能
-- 新增：box_id(设备唯一编码，用于管理设备)，设备ESP32，会同步IP，管理地址（http://esp32wifiip:port）,设备名称、类别、设备中的传感器组成（光敏、oled、LED、人体红外、DHT22……），需要对传感器引脚做标注和管理。
-- 列表：橱窗列表模式，1行2列，6/页

# 任务3
完成后台侧-管理系统的功能开发：
1、模型引擎
- 支持新增/删除/修改/查询/分页 （6/页）功能
- 列表：橱窗列表模式，1行2列，6/页
以下为大模型参数信息，你需要根据参数信息通过OPENAI API实现模型管理。
```
api_key="sk-aigc-74417635957c145bcc72f4687569e4e07b2c0b43"
api_url="https://aigc-api.aitoolcore.com/api/v1"
model_name="qwen3.5-flash"
```

- 支持模型测试（对话模式）
- 模型设置默认，后续有所请求均使用该模型
- 支持模型参数调整，用户可以在管理界面调整模型的参数，如温度、最大token数等
2、接口管理
- 支持新增/删除/修改/查询/分页 （20/页）功能
- 实现第三方接口和管理功能：天气、音乐、新闻等接口
- 支持接口测试和连通性检查

# 任务4
完成后台侧-管理系统的功能开发：
1、AIOT服务器管理
- 参考D:\Wireless Sensor shixun\project\CDUTAgentOS\dist\Server.py文件中的代码，实现一个TCPServer模型，并通过AIOT服务器管理功能实现新增/删除/停用/启动/编辑/分页等功能。
- 采用橱窗列表模式，1行2列，6/页
- 新增时，支持设置端口，AIOT服务器名称，保存后，支持启用服务，后台自动启动TCPServer，用于接收ESP32的连接，
并获得esp32的数据。
- 列表中需要显示链接上线的设备数，及设备ID。
2、优化AIOT设备管理
- 对连接成功的设备进行可视化状态提示/标注

# 任务5
完成后台侧、管理系统的功能开发
1、新增数据报表统计模块
- 将AIOT服务器+设备生成的历史记录通过列表进行统一管理，需要记录谁在什么时间对设备做了什么，以及设备的主动上报的情况记录
- 利用Echarts技术栈实现报表功能
- 布局采用混合布局，实现独立风格的数字报表统计和数据列表页面
- 数据需要实时记录更新
2.数字孪生模块
- 该模块暂时不实现，后续任务再完成

# 任务6
完成后台侧-管理系统的功能开发：
1、完善升级接口管理模块：
- 天气接口形成以下格式，需要兼容：
```
https://wttr.in/{城市名}?format=j1
如：
https://wttr.in/chengdu?format=j1
https://wttr.in/成都?format=j1
返回JSON数据
```
2、/uniapp/下放置的是移动应用框架，混合开发模式，你需要开发实现移动端app:
- 移动端框架为：
```
uni-app框架
支持uvue（uni-app x）
vue3版本
```
- 首页：参考qwen/豆包app 的界面布局，实现与大模型对话的功能，支持：
---- 大模型流式响应
---- 语音输入
---- 语音合成播报 （后续在模型引擎中新增TTS模型后实现）
- 设备：
开发要求：
- 接口需要实现天气预报接口接入，并在表中新增相关接口数据
- uni-app需要开发实现功能要求中的模块。